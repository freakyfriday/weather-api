describe("Weather Controller", function() {
  var weatherController = require("../../controllers/weather");

  it("should retrieve weather forecast by long/lat", function() {
    let longitude = -71.0589;
    let latitide = 42.3601;
    //expect(weatherController.getWeatherByLonLat(latitide, longitude)).toEqual({});
  });

 
});
